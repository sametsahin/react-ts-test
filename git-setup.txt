create a repo on github
Setup github on your env
    git init
    create .env and put it into the .gitignore
    git add .
    git commit -m "first commit desc"
    git branch -M main
    git remote add origin https://github.com/sametsahin/react-ts-test.git
    git push -u origin main

check status of your project.
    git status

